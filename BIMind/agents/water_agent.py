class WaterAgent:
    def run(self, model):
        # MVP: solo ejemplo conceptual basado en el modelo
        return {
            "type": "Water",
            "daily_consumption_m3": 12,
            "treatment_required": True,
            "details": "Basado en el volumen de ocupación estimado del modelo BIM"
        }
